// Placeholder page — Sent (/sent)
// A later phase will replace this with the real Sent campaigns UI.

export default function SentPage() {
  return (
    <main>
      <p>Route: /sent</p>
      <p>Placeholder — Sent campaigns UI will be implemented in a later phase.</p>
    </main>
  );
}
