// Placeholder page — Compose (/compose)
// A later phase will replace this with the real Compose campaign UI.

export default function ComposePage() {
  return (
    <main>
      <p>Route: /compose</p>
      <p>Placeholder — Compose campaign UI will be implemented in a later phase.</p>
    </main>
  );
}
