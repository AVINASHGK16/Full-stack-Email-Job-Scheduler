// Placeholder page — Login (/login)
// Phase 9.2 will replace this with the real Login UI + Google OAuth button.

export default function LoginPage() {
  return (
    <main>
      <p>Route: /login</p>
      <p>Placeholder — Login UI will be implemented in Phase 9.2.</p>
    </main>
  );
}
