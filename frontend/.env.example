# Frontend environment variables
# Copy this file to .env and fill in real values.
# Variables must be prefixed with VITE_ to be accessible in browser code.

# Backend base URL used by src/services/api.ts
# During local development the Vite dev proxy forwards requests automatically,
# so this variable is mainly needed for production builds.
VITE_API_BASE_URL=http://localhost:5000
